A Python program which takes a bunch of text files and creates an inverted index from them. 
This version takes the directory from the user as the input.
The index file is a CSV file instead of text, which makes it more searchable.